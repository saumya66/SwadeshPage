import "./stat.css";

import React from "react";

const Stat = (props) => {
	return (
		<div className="stat">
			<h5 className="stat-value">Whitefield</h5>
			<p className="stat-desc">location</p>
		</div>
	);
};

export default Stat;
